export const metadata = {
  title: 'Search Results | Torque Block',
  robots: {
    index: false,
    follow: false,
  },
};

export default function SearchLayout({ children }) {
  return <>{children}</>;
}
